name: Micro1 - Idempotencia CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'services/idempotencia/**'
  pull_request:
    branches:
      - main
    paths:
      - 'services/idempotencia/**'

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: notification-system/idempotencia
  EKS_CLUSTER: notification-system-eks
  K8S_NAMESPACE: notificaciones

jobs:
  # ─────────────────────────────────────────
  # JOB 1 — BUILD Y TEST
  # Corre en cada push y pull request
  # ─────────────────────────────────────────
  build:
    name: Build y Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache de Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('services/idempotencia/**/*.gradle*') }}
          restore-keys: gradle-

      - name: Compilar y testear
        working-directory: ./services/idempotencia
        run: ./gradlew build --no-daemon

      - name: Publicar resultados de tests
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: services/idempotencia/build/reports/tests/

  # ─────────────────────────────────────────
  # JOB 2 — BUILD Y PUSH DOCKER
  # Solo corre cuando el merge llega a main
  # ─────────────────────────────────────────
  deploy:
    name: Build Docker y Deploy
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login al ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build y Push imagen Docker
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        working-directory: services/idempotencia
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Configurar kubectl
        uses: aws-actions/amazon-eks-action@v1
        with:
          cluster-name: ${{ env.EKS_CLUSTER }}
          region: ${{ env.AWS_REGION }}

      - name: Deploy en EKS
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          kubectl set image deployment/idempotencia \
            micro1=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -n ${{ env.K8S_NAMESPACE }}
          kubectl rollout status deployment/idempotencia \
            -n ${{ env.K8S_NAMESPACE }}